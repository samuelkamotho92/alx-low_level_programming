#include<stdio.h>
/**
 * main -Print digits
 *
 * Return:Always 0 (success)
 */
int main(void)
{
	int digit;

	for (digit = 0;digit < 0; digit +++)
		putchar(digit + '0');
	putchar('\n');

	return (0);

}
